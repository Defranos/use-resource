{
  "name": "root",
  "private": true,
  "scripts": {
    "publish": "lerna run tsc && lerna publish",
    "compile": "lerna run tsc"
  },
   "workspaces": {
    "packages": [
      "packages/**"
    ]
  },
  "devDependencies": {
    "@types/node": "^17.0.20",
    "lerna": "^4.0.0",
    "typescript": "^4.5.5"
  }
}
